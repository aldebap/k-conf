{
    "scenario": "05.9",
    "description": "command list upstream targets",
    "option": "-verbose list upstream-target --upstream-id=${UPSTREAM_GUID}",
    "expected-result": {
        "status": 0,
        "output": "^http response status code: 200 OK$",
        "format": "regex"
    }
}
