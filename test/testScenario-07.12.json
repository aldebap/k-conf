{
    "scenario": "07.12",
    "description": "command delete upstream target without id parameter",
    "option": "delete upstream-target --upstream-id=${UPSTREAM_GUID}",
    "expected-result": {
        "status": 255,
        "output": "[error] missing upstream target id: option --id={id} required for this command",
        "format": "string"
    }
}
